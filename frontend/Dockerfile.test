# Frontend - TEST
# frontend/Dockerfile.test
# Builds an image that runs the Jest suite with coverage. No backend or
# browser is required; network calls are mocked in tests.
FROM node:20.11.1-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm install

COPY . .

CMD ["npm", "run", "test:coverage"]
